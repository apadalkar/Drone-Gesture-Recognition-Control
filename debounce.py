class Debouncer:
    """
    Debounces/smooths gesture input to prevent flickering between commands.
    Call update(gesture) with the current gesture; returns the stabilized command.
    """
    def update(self, gesture):
        # TODO: Implement debouncing logic
        return gesture 